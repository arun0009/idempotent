idempotent:
  dynamodb:
    endpoint: http://localhost:8000
    table:
      name: Idempotent
      create: true
    use:
      local: true
  aws:
    accessKey: key
    accessSecret: secret
    region: us-east-1
